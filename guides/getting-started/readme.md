# Getting Started

This guide walks you through setting up kube_engine, a Rails Engine for managing Kubernetes on Kubernetes.

## Install

```ruby
gem "kube_engine"
```

## Overview

kube_engine is a Rails Engine that provides Kubernetes management capabilities within a Rails application. It builds on the kube ecosystem to provide a web-based interface for cluster operations.
